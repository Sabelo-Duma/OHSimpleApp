# Enable HTTPS only when needed for local network access (set to false for Codespaces)
# HTTPS=true
# Make the dev server accessible from network
HOST=0.0.0.0
# Disable browser auto-open
BROWSER=none
# Fix WebSocket connection for Codespaces
WDS_SOCKET_PORT=0
# Allow connections from Codespaces proxy
DANGEROUSLY_DISABLE_HOST_CHECK=true
